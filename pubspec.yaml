name: focusflow

description: A modern productivity app with tasks, habits, timer, planner, and analytics.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.0.5
  intl: ^0.19.0
  path_provider: ^2.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/
